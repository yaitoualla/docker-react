sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t yaitoualla/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true yaitoualla/docker-react npm run test
